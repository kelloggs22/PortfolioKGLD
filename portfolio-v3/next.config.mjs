/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Se você tiver configurações de imagens ou domínios (como do Framer ou Cloudinary), 
     elas entram aqui dentro. */
};

export default nextConfig;